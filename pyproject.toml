[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "moe-orbit-prefetch"
version = "0.2.0"
description = "Full open Object A: MoE orbit prefetch + sparse DeepSeek runtime (MIT; weights not included)"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "andreyivan4enkov" }]
keywords = [
  "mixture-of-experts",
  "moe",
  "expert-prefetch",
  "deepseek",
  "dynamic-inference",
  "safetensors",
  "sparse-moe",
]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Science/Research",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
  "numpy>=1.24",
]

[project.optional-dependencies]
examples = [
  "torch>=2.1",
  "safetensors>=0.4",
  "huggingface_hub>=0.23",
]
runtime = [
  "torch>=2.1",
  "safetensors>=0.4",
  "huggingface_hub>=0.23",
  "transformers>=4.40",
]
bench = [
  "torch>=2.1",
  "safetensors>=0.4",
  "huggingface_hub>=0.23",
  "transformers>=4.40",
  "psutil>=5.9",
]
dev = [
  "pytest>=7.0",
]

[project.urls]
Homepage = "https://github.com/andreyivan4enkov/moe-orbit-prefetch"
Issues = "https://github.com/andreyivan4enkov/moe-orbit-prefetch/issues"

[tool.setuptools.packages.find]
where = ["src"]
